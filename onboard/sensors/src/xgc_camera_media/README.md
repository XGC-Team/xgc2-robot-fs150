# xgc_camera_media (robot-side copy)

Canonical script: `xgc_camera_driver` / `xgc_native_v4l2_rtp`
in `products/ros1/driver/camera`.

This copy exists so the vehicle can run before that package is
re-released. Prefer `rosrun xgc_camera_driver xgc_native_v4l2_rtp`
when APT has it.
