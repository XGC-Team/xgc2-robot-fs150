# legacy/camera_ros

Field V4L2+OpenCV publisher used to get a first image on rk356x.
It is **not** the product driver. Do not launch it.

Use `fs150_onboard_sensors/camera.launch` (native V4L2 -> Media Edge).
